# Netflix.AI Assets

This directory contains assets for Netflix.AI.
All icons are rendered inline via SVG to ensure zero external loading dependencies and fast, error-free rendering.
Fallback images and posters are dynamically mapped.
